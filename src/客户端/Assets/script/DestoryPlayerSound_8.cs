using UnityEngine;
using System.Collections;

public class DestoryPlayerSound_8: MonoBehaviour {

	// Use this for initialization
	void Start () {
		Destroy (gameObject, 8.0f);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
